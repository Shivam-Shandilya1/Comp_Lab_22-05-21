#include <stdio.h>
#include<stdlib.h>
#include <ctype.h>
int main()
{
int bin,p,q,r,x;
bin=1;q=0;
printf("Rows you want to input:");
scanf("%d",&r);
printf("\nPascal's Triangle:\n");
while(q<r)
{
    for(p=40-3*q;p>0;--p)printf(" ");
    for(x=0;x<=q;++x)
    {

    }
}

return 0;
}
